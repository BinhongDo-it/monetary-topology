# OBJECTIONS.md：已经查过的怀疑

**建于 2026-08-16。** 立这份文件的理由很直白：session 之间没有记忆，于是同一批反驳被
反复提出，每次都要重新查一遍出处、重新得出同一个结论。**这份文件是那个查证的缓存。**

---

## 用法与规则

**给审查者（人或模型）的规则，三条：**

1. **提反驳之前先查这份表。** 表里有的，记录里已经有答案和出处。
2. **重提已结清的条目，必须先指出记录的答案**哪一步错**。** 说不出哪一步错就不要提。
   「但是另一方面」「你是否考虑过」不算指出。
3. **第五节是仍然开着的。** 那些该提，而且这里就是清单，不必自己重新发现一遍。

**这份表不是盾牌。** 新的反驳照提。它挡的只有一件事：把已经查过的东西当成新发现再讲一遍。

**加条目的规矩**：每一行必须带**出处**（文档 + 节号）与**裁定**。没有出处的不许进表。
条目只增不删；裁定变了就改「裁定」栏并注明日期，原文留在「还剩什么」里。

---

## 一、数理臂 / B 轨

| # | 反驳 | 出处 | 裁定 | 还剩什么 |
|---|---|---|---|---|
| A1 | 价格是一元向量，`log R_ij = log p_j − log p_i` 是梯度，闭环恒为零，所以卷二讲的是空集 | `b1_setup.md` §1–§2 | **不成立。** 一元记法下可积性是**记法的定理**不是经济结论。相关的量是 `P(a, j)`，两指标。举证责任反转到「获取条件与你是谁无关」那一侧 | 无 |
| A2 | 环和非零是恒等式，因为修改/重签就是改条款 | `b1_setup.md` §3、§7；`b3_cip_slice.md` B3-2 | **不成立，恒等式在另一边。** 「a gradient field returns zero on every loop, in every metro, in every year, **by identity**」。条款改多大都不影响，梯度场照样归零 | 只剩**节点同一性**（回到 `current` 但利率变了）。`b8_fannie_slice.md` §5 的 B8-3 使这个反驳不必赢 |
| A3 | 这些是交易成本与风险溢价，不是套利 | `b1_setup.md` §7 | **不成立。** 「A critic who wants to save integrability has to argue the loop sum is **zero**, not that it is deserved.」拆解缺口是另一个经验问题 | 无 |
| A4 | 那就把价格定义成风险调整后的有效价格，又可积了 | `b1_setup.md` §7 | **不成立。** 这要求存在一个只依赖位置的标量能复现每个 agent 的条款。若存在，框架就错了，**而那正是待检的经验问题**，不能假定 | 无 |
| A5 | 套利会被竞争掉 | `b1_setup.md` §7 | **不成立。** 只在**存在的边**上被竞争掉；构造的全部内容就是收尾那条腿对付钱那一方缺失。该反驳假定了它要检验的结论 | 无 |
| A6 | 租比买便宜，你的环跑反了 | `b1_setup.md` §3、§7 | **不成立且对框架有利。** 可积性要求环和**为零**不是为正。一个在匹兹堡为正、在圣何塞为负、随利率翻号的量不是任何位置标量的差。**离散度就是证据** | 无 |
| A7 | 你的十倍回报是一次性重定价不是结构性楔子 | `b1_setup.md` §7 | **部分成立，已内建应对。** canonical loop（同一住宅两个 vintage）完全不依赖 revaluation；且**多个不重叠窗口**是跑前钉死的区分检验 | 该检验在 B8-2 上首次真正执行 |
| A8 | 被排除的人可以买便宜房子 | `b1_setup.md` §3 tier 指标 | **不成立。** 排除发生在**层级**上不在租/买之间，层级不共享回报分布 | 在层级上重述该反驳是欢迎的，那时它是关于层级准入是否被门控的经验主张 |
| A9 | 你在讲 FX | `b1_setup.md` §4 | **不成立。** 无 FX 数据进入主线。FX 只作可选的教学插图，不携带结果 | B3 用的是 CIP，那是 slice summand 的载体，不是 FX 头条 |
| A10 | 这就是价格歧视，早有人研究 | `b1_setup.md` §7 | **不成立。** 主张是关于**表示**的后果：条款两指标之后，标量价格水平不是忠实摘要，去中心化配置论证赖以成立的加总步骤不可得 | 无 |
| A11 | 方块复形是不必要的装饰 | `b1_theorem.md` §12.2；`B2_方块复形裁定_v1.md` | **成立，且项目自己证的。** within share 是 1-skeleton 量，对 `C₂` 的任何选择恒等不变 | 已入账。`PROJECT_PLAN.md` §21.5：B7-6 是这条裁定的正面检验（起作用的是 `H`，属 1-skeleton） |
| A12 | Theorem 3 是代数恒等式，而 B2 的主数字就是它，所以数理臂坍缩成方差分析 | `b1_theorem.md` 开头 Load-bearing order（2026-08-15）、§13.4 | **成立，项目自己认领并重排了承重序**（Cor 1 → Cor 2 → Thm 3）。外部审查独立复现同一诊断，**不重复计功** | 无 |
| A13 | 不可替代内容全在 slice 与 H⁰，而已跑完的数据一处也碰不到 | `b3_cip_slice.md` §11 | **事实错误。B3 已跑完并通过**（G10 `√Z` 30.9→45.6 bp，噪声底 2.8–3.7 bp）| 成立范围只到「已跑完的**房贷**数据碰不到」，那正是 Corollary 2 的收窄块。空格是「slice × 带分层」，即 B8 |
| A14 | A3-8′ 的阈值是循环：自选星形载体 ⇒ `b₁(G)=0` ⇒ 由 Thm 2 必然推出点预言 ⇒ 同载体验证 | `b1_setup.md` §5（2026-08-15 追加块） | **收窄。** 项目自己抓到「结论对但理由错」。星形不是被迫的，是**当前测量表的性质**（七行里六行有 cash 在一侧） | **不是要辩护的循环，是要多测一行的空缺**：加一行非 cash 对非 cash 的边即破 |
| A15 | HMDA 够不到 slice 是因为它是横截面没有借款人 ID | `b1_theorem.md` Corollary 2 收窄块 | **理由说错了。** 真实理由是「**a cell is one edge**，故每 cell 的位置图两个顶点、`b₁(G)=0`」，这是**构造选择**不是 HMDA 的性质 | borrower-linked panel 与 `b8_fannie_slice.md` §2 的三角都不被该论证排除 |
| A16 | 出去再回来就是一个环 | `b1_theorem.md` Corollary 2 收窄块 | **不成立。** 在 `{rent, own}` 上往返因**反对称**恒为零。slice 需 `b₁(G) ≥ 1`，即至少三个位置、某一对之间两条路 | B8 的三角满足，B8-0a 拿往返做零校准 |
| A17 | ~~B7 秩 ≥ 2 说明房贷载体没有单一阶梯 → 不可主张~~ **【2026-08-16 反转】** | `b7_interaction_rank.md` §3.22；`results/b7_rank_draws50.json`；`PROJECT_PLAN.md` §23 | **原裁定作废。B7-6 `passed: True`**，细网格 2、粗网格 2，两个 null 一致。原先记的「细 2 / 粗 1」里那个粗网格是**索引乱序的产物**（见 A21） | **「没有单一阶梯」现在可以说。** 但须同时挂：**秩 2 不等于标准计量够不到**，Bai(2009) 的 interactive fixed effects 就是 r 因子的双向交互 |
| A18 | ~~那是因为粗网格分辨不出 2~~ **【2026-08-16 作废】** | 同上 | **原证据作废**：当时那个 B7-0b 跑在乱序网格上。**结论碰巧仍对**——修正后的粗网格直接读回 2，根本不必靠闸来证明它分辨得出 | 无 |
| A19 | 第二个维度就是 43% 的 QM 门槛 | `b7_interaction_rank.md` §3.22 | **由证据排除，比原先的「跑前围栏」强得多。** 悬崖是 `36-42` 对 `43-49` 的对比，补网格（十四个整数各自成类）**精确表示**它，而补网格读 **rank 0**，整条谱平在自己的噪声底上，**任何方向上都读不到任何东西** | 功效反驳已答掉：补网格的十四个整数类与细网格逐一相同，`null_max` 0.2405 低于细网格 0.3817，**更少受噪声限制** |
| A20b | 交互住在 DTI 的细整数分辨率里 | 同上 | **不成立，而且相反。** 两个方向都以 **97.9%** 载进保留五个公布桶、合并十四个整数的六层索引；**合并那十四个整数只花 2%**。整数分辨率什么也不载 | **交互整个是桶级对象。** 报 DTI 效应时不得暗示逐整数的分层 |
| A21 | 分区的组数、填充率、贷款数都对，判据都可满足，闸都通过，所以分区是对的 | `b7_interaction_rank.md` §3.21；`PROJECT_PLAN.md` §23.8 | **不成立，这是范畴错误第十二式。** `load_with_class` 按首次出现给代码，`design_from_loaded` 存字母序，两个分区函数按位置读，于是「粗网格」把 `<20%` 和 `49` 并进同一类。**所有计数量保持不变，每一道闸都通过。** 原话：a partition's name is not its membership | **解药：打印成员，不打印计数。** 乱序分区在计数里不可见，在名单里一眼可见。凡按位置索引的表，必须在算任何东西之前打印成员 |
| A20 | 挂牌可替代品（国债、上市股票）条款对所有人相同，所以那里 `π` 应当为零，可以拿来做零域 | `a3_asset_channel.md` §2 与 §3；`b1_setup.md` §5 | **不成立，两处都错。** A3 早已把它拆成两个对象：`γ_pay` 进 cochain 算 holonomy（`H¹`），`γ_gate` 是**对定义域的限制、一个洞**（`H⁰`，按构造在 cochain 之外）。而 terms 规则 `γ_{i,q} = γ̄_q(1 + κ(1−c_i))` 的原稿锚点就是**卷一·三的货币规模经济**：**数量本身让条款变成 agent 专属的**。挂牌市场上直接可观测（市场冲击对 ADV、大宗、融资分档、证券借贷、创设赎回只对 AP）。另 `b1_setup.md` §5 最后一行判合格投资者线是 **a pure hole，the edge is absent at any price** | **零域因此改设计，见 `PROJECT_PLAN.md` §22.11–22.13**：不是零域，是**规模分级域，零在它内部**。臣妾 2026-08-16 犯的错，记在这里 |
| A22 | **A3-3 / A3-4 / A7-A 均为构造恒等式，不该出现在任何「证据」表里**（⑫b 原文） | `experiments/a3_asset_channel.py` 模块 docstring 与 `assert_no_shared_code()`；`src/monetary_topology/asset.py` `loop_sum()` docstring；`RESULTS.md` A3 节 | **对 A3-3 与 A7-A 成立但已办；对 A3-4 事实错误。**（i）A3-3 **早已是 `DIAG`**，行文自带降级理由（`adjusted return = -log gamma` 由构造成立，机器精度读恒等式）；（ii）A7-A 全 leg 带 `diagnostic_only`，**渲染器整条跳过，根本不在 `RESULTS.md`**；（iii）**A3-4 不是恒等式**：两个数分别出自 `asset.py`（跑一整个带价格、流通、工资单、增发、供给上限、换手的经济）与 `product_graph.py`（只给条款矩阵与两个价格向量），`run_all.py` 以 AST 级 import 检查断言两者互不导入，`asset.py` 明写「若由同一套代码算，它们的一致就是恒等式，A3-4 什么也不能确立」 | **无。** ⑫b 那条对 A3-4 的指控由项目自建的护栏预先挡住 |
| A23 | ~~**B2 的主数字 = Thm 3 = 代数恒等式，B1-6 是拿机器精度读它**~~ **【2026-08-16 同日作废，本行是 A12 的重提】** | `b1_theorem.md` 开头 Load-bearing order 块（2026-08-15）；`PROJECT_PLAN.md` §21.7、§24.3 | **不成立，且这一条已由 A12 结清，「还剩什么」栏写着「无」。** 臣妾原写「行文欠一句：`RESULTS.md` 里 B1-6 的标题读起来像 Thm 3 被确认」，**那个论证错在把 `RESULTS.md` 当成论证的载体**。`RESULTS.md` 是机器台账，论证的分量由**承重序**决定，而承重序 2026-08-15 就定成 Cor 1 → Cor 2 → Thm 3 并**在 `b1_theorem.md` 正文里逐字披露**（「a reader who wants to dismiss it can say the framework renamed an analysis of variance, and at the level of arithmetic be right」） | **无。行文已逐字节还原，不改。** 教训：重提前四节的条目，须先指出记录的答案哪一步错，臣妾没做到 |

---

## 二、实证臂 / 指纹

| # | 反驳 | 出处 | 裁定 | 还剩什么 |
|---|---|---|---|---|
| B1 | 样本外预测必须是未来才算预测力 | 纪律 2 | **不成立。** 构造纯出理论、未对任何国家数据拟合 → 跑在哪国都含预测力。F13（17 国 150 年）与 JP-P1（干净预测 PASS）都是 | 无。**臣妾在 2026-08-15 犯过这个错，记在这里** |
| B2 | 制度形态假说在部门层没有留下指纹 | 洞察 14、关线段（正确版）；`PROJECT_PLAN_FINGERPRINTS.md` §4.2 | **不成立。** 四次尝试：两次不可判定（F6e v2、F15b）、一次未开、一次是器具读数。**真正的否定一次都没有** | 结论 51 那处措辞须撤回（第五处出现在 `HANDOFF_FINGERPRINTS.md` §4） |
| B3 | 跨国样本不够，多找几个国家就行 | 结论 53；`ri_ceiling.py` | **不成立。** 同步性高的面板上扩样本可能一分钱信息都买不到。`h ≈ 50` 上十七国只有 5.3 个独立窗口 | 该 horizon 结构性关闭，**不得以样本不够为由再开** |
| B4 | 找一个更好的 MRIO 就能做跨国边死亡 | 结论 48 附则 | **不成立。** 多国库的接缝与过度平滑**构造上互斥**，压住一个加重另一个。缺陷属于这个类别本身 | 只关闭了跨国横截面形态，**国内带日期事件研究形态没关** |
| B5 | 换个覆盖更全的协调化数据库就能解决 LME 覆盖 | 第十式 | **不成立。** 口径越统一的器具处理变量变异越小，因为统一口径的动力来自共同统计法规，而那本身是协调型制度的产物 | 解药是把处理变量连续化或搬回国内 |
| B6 | 日本的边不死 / 日本代谢没有选择性 | 复检 k、洞察 14 | **两条都已撤回。** lift 的跨国序关系与三国**发布表印制分辨率**的序关系完全同序 | 可写的只有共同分辨率下「细边比粗边更容易死」，**不得跨国比较** |
| B7 | 通胀/名义增长把细边推过阈值，所以收缩是机械的 | 日本站 | **不成立。** 日本 1994–2021 中间流量 406→484 兆円近乎持平，三方向照样全中 | **离岸化混杂未排除**，见第五节 |
| B8 | 顶端集中说明大边越来越大 / 在位者锁定 | 结论 34 | **不成立，且反向。** 浮动 top1% 上升而**固定基年 top1% 集合的份额下降**，约四成成员被替换。**上升的是位置的价值，不是占据者的份额** | 这同时**切断了「垄断/在位者锁定」这一类对手解释**，框架在这里与它们分道 |
| B9 | 存量棘轮的货币归因没测出来，再试一次设计 | 复检 i 四份 | **不成立。** 不是没测出来，是**测出了显著反号**，且不由任何杠杆承担（逐十年/单格/单年/逐国留一全无翻号） | **归因不得写入原稿，任何版本。** 反号本身的解释仍开着，见第五节 |
| B10 | F16 说明相对量层也会失败 | HANDOFF §4 共同诊断（2026-08-14 改写） | **不成立。** 判别式看**赌注下在哪一层**不看结果变量长什么样。F16 的赌注是「哪些年份斜率更陡」，属**时点层**，是那一族的第四例 | 楔子这个可观测量本身没受损，F12b P1 与 F13 不受影响 |

---

## 三、原稿层（卷内正文已经接过的）

| # | 反驳 | 正文出处 | 裁定 |
|---|---|---|---|
| C1 | 那 1 人花掉了 15，钱总得流到某处，最终会到 40 人手里 | 卷一·一「承重反驳」 | **【2026-08-16 改弱形式，陛下裁准】**同层内支出在功能上**接近**储蓄：账面速度非零，而向层外的净位移远小于账面速度所暗示的量，**且两者的比值在上升**（F7：Z.1 链长 1.01 → 2.13，1980 年后 z = 7.94）。**「拓扑上零位移」那个水平主张已撤下，本稿不再承担它。**方法论后果不变：光有数量不够，必须指定邻接矩阵 |
| C2 | 但 MV=PQ 恒成立 | 卷一·八 | 它太一般所以什么也没说：把拓扑积分掉了，而拓扑是唯一要紧的东西 |
| C3 | Boskin 只处理商品，服务另说 | 卷一·十 | **该分界不成立，已撤回。** 医疗是 Boskin 质量调整论证的重头。核实过程反而产出两条对本节更有利的事实 |
| C4 | 1929 年直接持股者是少数，股市渠道不重要 | 卷一·十七 | **用错了判据。** 本框架自己的判据是**中心性**不是基数 |
| C5 | 坏账吃掉的权益不服从守恒 | 卷一·十四 | **原写法是错的，已改。** 吃掉的是**账面权益**，它守恒；会长回来的是**市值**，由不守恒的那一层出资重填 |
| C6 | 财富分布指数发散、无均衡 | 卷一·十六 | **框架不需要发散，只需要黏着。** 发散是可争的经验主张，争赢争输都不影响本节要说的「递归没有基例」 |
| C7 | 自动化两百年来没有减少总劳动需求（lump of labor） | 卷三·七 | **该反击成立但打不到。** 本节主张的是**人均总工时需求**下降，是不同的量。三条机制里注意力上限是**上界论证**不是需求论证 |
| C8 | 企业会做差异化，所以竞争不产生单一栽培（Hotelling / 垄断竞争） | 卷二·五 | 差异化发生在**标量所奖励的维度上**，产出的是**表层特征的多样与结构的收敛**。眼镜是标本 |
| C9 | 小盘股跑赢大盘（Fama-French SMB），赢家通吃不成立 | 卷二·四 | **主张已限定在债务与资本可及性上，不在股权回报上**，不在那里开战 |
| C10 | 供给创造自己的需求（萨伊） | 卷三·四 | 萨伊是 **claim 层**的主张，卷一已证伪；本节是 **capability 层**的主张。两者不同 register，可同时成立 |
| C11 | 单位研究者产出率在下降（Bloom 等 2020） | 卷三·四 | **原稿自认「须正面处理而非绕开」且「需要独立论证，不能假定」。此欠条未还**，见第五节 |
| C12 | 长视野投资在公开市场上并非不可能（亚马逊、生物科技、特斯拉） | 卷二·六 | **原表述过强已收回。** 改为「长视野需要一个能在无盈利期支撑估值的叙事，而该叙事只对少数公司可得」，故约束是**不对称绑定**不是普遍禁止 |
| C13 | 财富集中跑赢收入集中还有 r>g、组合构成、遗产等分量 | 卷一·五 | **框架不主张解释全部 R²**，只主张时滞套利是其中一个**可测的分量**，且它给出别的解释不给出的区别性预言（时点、指数化跨国变异） |
| C14 | **违约瀑布没钉事前时滞，任何时序都与它相容，故它不是证据** | 卷一·十八；登记 L3 | **成立，已按它办。**【2026-08-16 结清，由 O3 移入】开工前过闸：可用的转折配对只有**一对**，赌注落在**时点层**（已四连败），功效阶梯里比「四五点短序列」那一行还少；**「多州横截面复制」在设计上无效**，五十个州带着同一批季度，全国性转折在逐州留一下结构上不可能被剔掉。**故 L3 不开站**，执行它自己写下的另一半：**瀑布降为并列截面，删掉「逐级传导」的措辞**。救法（各州驱逐禁令到期日期不同，改成带日期的制度变迁事件研究）见 `PROJECT_PLAN_FINGERPRINTS.md` §14.3.4 |
| C15 | **卷一·十八四个截面的选择本身触到纪律 1**（特征搜索锚定结论） | 卷一·十八；纪律 1 | **成立，已按它办。**【2026-08-16 结清，由 O15 移入】四个截面不是从事前指标清单里选的，挑中的都在极值上，唯一不在极值的房贷被事后解释；学生贷款、优级车贷、企业破产未出现也未说明。**处置：本节降为例示，不作为独立证据计入**，替换文本见 `PROJECT_PLAN_FINGERPRINTS.md` §14.3.2（九）。要升回证据，须先写下事前的家庭债务指标全清单再逐条报读数 |
| C16 | **存量口径的 90+ 逾期率在上升，说明当期还款行为在恶化** | 卷一·十八 | **不成立，且这一击打在原稿头上。**【2026-08-16 新增】纽约联储 2026-08-11 拆掉了这个读法：核销发生在逾期 120 至 180 天，余额从贷款人账上剔除、**同时退出分子与分母**，但仍报给征信局，于是留在存量口径里；而**核销一年后仍在申报的比例由 2004–2012 的约 40% 升到 2024 的约 80%**。剔掉核销余额后，存量、流量、Call Report 三口径完全对齐。**本稿两格（信用卡与车贷的 90+ 存量份额）因此撤下，换流量口径**（信用卡 6.93% → 6.97%，车贷 2.93% → 3.00%，两年基本持平）。**60 天口径落在核销点之前，次级车贷那一段不受影响** |
| C17 | **K 型分化（房贷违约低而车贷信用卡高）说明政策保护了有资产者** | 卷一·十八 | **两段处置。**（i）**用存量口径读它不成立**：高的那一侧吃核销滞留污染，而房贷走止赎、**止赎同样计入 severely derogatory**（HHDC 数据字典原文），故房贷并非结构上免疫；HHDC 只报 severely derogatory 合计、不按贷款类型拆，**两侧各被污染多少用公开数据量不出来**。（ii）**改读流量口径即可绕开**：房贷 1.52% < 车贷 3.00% < 信用卡 6.97%（新进入 90+ 的年化转化率，**同为 2026 Q2**），**K 型方向在干净仪器上成立**。**但「政策保护了有资产者」是解释不是读数**，那个序与「抵押品可回收性与追索结构不同」「房贷借款人被审批筛过一道」同样相容。**本稿只报序，不报解释。**【2026-08-16】 |

---

## 四、方法论与做法层

| # | 反驳 | 出处 | 裁定 |
|---|---|---|---|
| D1 | 跑后重设计 / 看到数据再写预注册是学术不端 | `CLAUDE.md` §8；纪律 3 | **不成立。** 没有产生 evidence、没有影响 evidence selection、没有影响最终 inference 的废弃设计，不必提升到与正式实验同等的 disclosure 地位。**按现实修正理论正当** |
| D2 | 撤回太多，说明框架在崩 | `PROJECT_PLAN_FINGERPRINTS.md` §5 | **不成立。** 撤回多是健康读数。该担心的是撤回的**方向分布**，不是数量 |
| D3 | 判据是事后调的 | 各站预注册 + §3.9 类的全映射表 | **不成立。** 结局跑前全映射，未命中归「混合」不事后重编。已有多次实例（F14、复检 k、B7-6） |
| D4 | 数学不够正统 / mathiness | 方法论前置 | **无关。** 四条对应性标准是可用的判别式。且项目自己在 A3-3、A3-4、A7-A、Thm 3 上主动降级过恒等式读数 |
| D5 | 一个人做全套所以不可信 | — | 与成立与否无关，只与速度有关 |
| D6 | C8-1a 的控制组没落在 1.00，注册原文写着「下面全部不可读」，所以 C8 整块作废 | `results/b8_c8_arithmetic.md` C8-1a；`claude-docs/B8_C8-1a裁定_v1.md` §3；《研究规矩审核表 v1》§0.1 与 X3 | **部分成立，判定的形状已换掉。** 偏差是真的（中位 +1%，**过半安静月份的余额下降严格大于重算的计划本金**），但「全部不可读」是二值判定而**同一个偏差对两个下游差十一个数量级**：对 C8-1 是 1 点对 15–20 点的判别缺口且同向作用于两个分支，可忽略；对 B8-0a(i) 是 1e-4 到 3e-2 对 1e-15 的浮点容差，致命。按 X3 废 0.50 阈值的先例换成分下游判定，**旧判据的数一并留档** |
| D8 | B8-0a(i) 原注册要求「精确回零到浮点容差」，现在改成两条，这是把失败的判据改到通过 | `b8_fannie_slice.md` §16；`claude-docs/B8_0a闸容差裁定_v1.md` §3.4；《研究规矩审核表 v1》R01 | **不成立。B8-0a(i) 从未跑过，没有旧数可洗。** R01 要守的两条是「同一批数据上改判据必须重跑并双报」与「不得同次混动判据与取数口径」，本次两条都不适用。被改掉的是一个**隐含前提已被 1.4 亿个月份否掉**的判据：闸的样本定义里藏着「余额走计划表」，而约四分之一的安静履约月份不走。§14.5 自己拆 0a 时那句话逐字适用，**这是把 §10 第 1 步该找出来的歧义找出来** |
| D9 | 既然冻结与欠缴在 `V` 上不可区分，`ω` 就测不了任何东西 | `b8_inputs_availability.md` §6.2.7.4 | **过度断言。** 它定的是 **B8-1 噪声底的一个下界**，而且是不由样本量决定的下界，**已登记为结构性限制并要求逐处引用**。它不影响 B8-3：那是两条**已实现**路径的对比，冻结在两侧同分布，差分里消掉。也不影响 B8-0a(i-a)，那条按支付规则性收窄，样本里没有冻结 |
| D10 | B8-0a(i-a) 判 PASS，但 `max ratio` 六档全钉在 0.400，所以那道闸是空的 | `b8_inputs_availability.md` §6.2.11.1；`b8_fannie_slice.md` §16.7 | **一半成立，已按成立的那一半改写判定的引用规矩。** 比值那一半确实是空的：路径容差与符合度的界共用同一个 `1/B`，上限 `(1+(1+i)^(k+1))/((2+i)(k+1)+(1+i)^(k+1))` 在 k=1、票面 3.75–7.5% 上是 0.4003–0.4005，而六档 `k` 中位数全是 1，**符合度那一半在数学上被路径那一半蕴含**。**但闸不是空的：判别力全在合格计数上**，90,115 个环里 53,781 个合格，而月供估错 1% 会把终点移动 $30.55 对着 $0.0101 的容差（三千倍），合格计数会塌到接近零而不是掉几个百分点。**处置取「照实写下来并要求逐处引用」这一支，不取「重设一个新判据」**，因为后者要在同一批数据上改判据 |
| D11 | quiet 过滤器改了口径，这是在同一批数据上动取数惯例 | `b8_inputs_availability.md` §6.2.10；`claude-docs/B8_0a闸结果与报送惯例_v1.md` §3；R01 | **不成立，但要守 R01 的双报。** 改的不是判据是**缺陷**：旧过滤器让「余额正 → 余额零」这一对进安静样本，`obs` 等于整个余额，而余额归零是终止即事件，按构造它从来就不该在里面。R01 要守的是「同一批数据上改了就必须重跑并双报」，**已登记四个脚本待重跑双报**，旧口径由 `require_cur_positive=False` 保留可逐位复现，三个手写过滤器的旧脚本其数原样留档。**同一次改动里没有同时动判据**：闸的容差是上一轮独立改的，双报也已落 |
| D12 | 既然 `upb[当前行] > 0` 在六档上一个对都没删，那它就是无意义的改动，应当撤回 | `b8_inputs_availability.md` §6.2.10.2；`results/b8_quiet_delta.md` §4 | **不成立，但前一半的事实是对的且已入档。** 更正确实不承重，**地位由修复降为防呆，这一条已经逐处改写**。不撤回的理由是它的冗余有名字且可变：挡住结清对的是**结清行的剩余法定期限为空**（六档漏网 5 / 2 / 5 / 1 / 0 / 0）加票面利率为空（各漏几百），**而不是三重冗余**。延滞那条根本不是冗余，它在 2002Q1 拒掉 99.25%、到 2019Q1 只拒 27.39%，**新 cohort 的终止行照常报延滞**。所以只需两列开始报数这条防呆就变成承重的，代价是一次比较。**「当前无效」不等于「可以拿掉」，除非那个使它无效的条件是不可变的，而这里它正在变** |
| D13 | C9 排掉空白层与 `U` 之后 B8-4a 才有网格，这是调样本口径来让闸通过 | `b8_inputs_availability.md` §6.4.4；`results/b8_c9_cells.md` §2.1；陛下裁定 2026-08-16 | **不成立，且双报已落。** 排掉的不是观测而是**非类的层**：空白是字段未报，是测量缺口不是 agent 类，§7 对测量缺口的形式本来就是排除加报数。**全层读数原样留在同一张表里逐格并排**，并标出哪几张网格的判定由这条裁定改变（`fico_llpa9`、`fico_llpa_coarse5`、`dti_complement15`、`fthb`、`purpose`），所以效果是可见的不是被吸收的。**另外两道限制同时生效**：`purpose` 的 `U` 识别依据只是布局文档，不满足 C0b 的行为识别标准，已标为「documentary basis only」且明写下游不许当已识别，其量为 14 笔；地板之外还有 §2.4，`ltv_llpa_coarse4` 与 `occupancy` 过地板但挂在房屋上，仍不是可用网格 |
| D14 | `dti_complement15` 十五层过闸而 `dti_coarse6` 六层不过，说明 C9 的地板对层数敏感，是个坏判据 | `b8_inputs_availability.md` §6.4.6；`b7_interaction_rank.md` §3.3 | **不成立，读反了。** 地板对**层数**不敏感，对**哪些层被合并**敏感，而那正是 §3.3 在 B7 的秩统计量上量出来的结论。两张在全层读数下都是 11、都卡在 `>60` COVID；complement 把 `>60` 并进「36-49 之外」吸收掉，coarse6 单独留着。**层更多的活、层更少的死，恰恰否掉「层数决定」这个读法。** 这是同一条结构性结论被一个纯计数、在另一批数据上独立重现，**比原来那一条强不是弱** |
| D15 | 曲线的构造规则实测不承重，那 §16.11 那两条就不必写，写了也是形式主义 | `b8_inputs_availability.md` §6.5.4；`results/b8_cmt_sensitivity2.md` | **不成立，而且理由不是形式。** 一个看不见的选择仍然要在跑前写下来，否则下一个人**分不出它是被看过还是被漏了**，而这两件事对后续判断的价值完全不同。更实的一条：**「够不到」这个结论是在曲线能进来的那批之外量出来的**，递延腿 703,504 行一个都估不出月供、一个都没进这次测量，所以 §6.5.4 的适用范围本身带限定，规则一旦到那批人身上是否仍不承重，**没有数** |
| D16 | 第一次敏感度读 267 到 7,110 倍、第二次读低八个数量级，同一个问题两个答案差十几个量级，说明这套测量不可信 | `b8_inputs_availability.md` §6.5.3、§6.5.4 | **前提对，结论反了。** 两个数不矛盾，它们是**两个不同对象**：第一次量 `log V` 单腿的极差，第二次量 `r` 与环和的极差，而 `r_month` 两条腿共用 `(i, n, d)`、年金因子代数上精确约掉，所以单腿的量到不了 `r`。**差十几个量级正是这个约掉的量级**，两次读数一起构成对它的确认。可指摘的是臣妾第一次量错了对象并据此下过结论，**那一次的件已作废留档、错因逐条入案**，而不是两次都不可信 |
| D7 | 那 1% 是代码误读，字段认错列了 | 同上 §5 | **不成立，被读数自己否掉。** 要让 1.7 亿行里**超过 25% 精确落进 [0.995, 1.005]**，字段 9、12、17 必须全对。**一个错列造不出 25% 的点质量。** 这比 C0b 那十八个锚点是更强的确认。`sched_principal` 的公式、`quantiles` 的分位定义、`rbin` 的分箱边界也各自逐行查过 |
| D17 | **修改三角的环窗口至今没有定义，所以 B8-1 之后的每一条都悬空** | `b8_fannie_slice.md` §17；`b8_0a_gate.find_clean_cures` docstring | **成立，已注册（2026-08-17）。** 概念层 §14.6 早已写过（last current before → first current after），**行级操作化也早已存在一份**：`find_clean_cures` 的 `t0/start/end`，残差跑 `t0+1..end`。§17 把它推广到修改与递延两臂，`find_clean_cures` 是「事件起始沿计数 = 0」的特例。钉死的关键三条：**窗口内部一个 `current` 行都没有**（否则那是两个环，正好抹掉 §5 的 B8-3 路径对）；**`t_M == t_B` 的环单独计数**（修改与自愈同一行，`leg 3` 是构造上的空，混进去会撑起「`ω₃` 实测近零」）；**段内两类起始沿的环从两臂都排除**（走的是 `deferred→modified`，§14.4 注册的五条边里没有它）。并分开命名 `ω(环)` 与 `Ω(路径)`，B8-3 读的是后者 |
| D18 | **CMT 的插值与超档规则未钉死，而三十年期在 2002-02 到 2006-02 之间不存在** | `b8_fannie_slice.md` §17.14–§17.16；`results/b8_cmt_sensitivity2.md` | **成立，已注册（2026-08-17）。** 插值取 **linear_in_tenor**（档位之间没有发行人约定，落到「不引入自由参数」的次级规则）；超档取 **cap**，**理由不是保守而是与 §16.16 已下的裁定一致**：CMT 由发行人定义、发行人没发就是没有，外推等于给一个发行人不卖的期限造价。缺口里的修改 1,506 / 50,958，2.96%，随引用一起走。**同时把 §16.16 那句「实测不承重」改准**：曲线唯一进得来的门是气球项，而带气球的行月供已知是 0、带气球的环六档全零，所以 4.974e-14 是代数抵消之后的舍入而不是效应量，正确说法是**今天够不着**，O24 解开之后必须重跑这张表 |
| D19 | `t_M == t_B` 占已注册环的 76.8%，那条 `modified → current` 边在其上给零个月，所以那 34,621 个不是三角 | `b8_inputs_availability.md` §6.6.8.2；`results/b8_c10_contract_move.md` §1；§6.6.7.2 的跑前映射 | **不成立，由 C10-1 判甲（2026-08-17）。** 那一行的合同**真的动了**：A 臂 `any` 0.6905–0.9986，与已知是延滞中再签约的 B 臂（0.6784–1.0000）**同量级，A/B 比 0.957–1.224**，两者都在噪声底的四位数倍上。**底本身同时验了判据**：清洁自愈 919,207 行上 `term` 精确读 0.0000，若「减一」这个基线错了，那九十二万行会整片亮起来。动的主要是期限不是利率（2012 后 `term` 近乎全体），`upb` 上跳 0.907–0.992 与 C8-4 相容。**残余：`ω₃` 只在 10,449 个环上可量，是被选择的子样本，每处引用必带** |
| D20 | `deferred` 这一层在 Fannie 上分不出来，§14.3 与 §14.4 互相矛盾 | `b8_inputs_availability.md` §6.6.11；`results/b8_c10_4_tier_carrier.md`；§6.6.10 的跑前映射 | **不成立，由 C10-4 判乙（2026-08-17）。两节都对，且一直都对。** 判据取 §14.4 自己的定义写成合取（`still` = 利率不动**且**字段 17 恰好减一）：**字段 63 读 0.0513，字段 108 与 ADR 码读 0.9966**，差 19.4 倍。108 与 ADR 码的首个上升沿在 **35,617 笔贷款上逐笔同行、零例外**，**那是 C0b 要的行为识别，比一个锚点强得多**（两个互不相邻的列，一个金额一个代码）。字段 63 与 108 **从不同行**，而 63 的指纹是 rate 0.4610 / term 0.8423，即 §14.3 leg 2 的修改。**错的只有实现切了哪一列，不是任何一段注册文字。** **残余**：重跑 `b8_loops` 与 C10；C11 未结（`V` 的气球项读不到 108 的递延余额） |
| D21 | 排掉带字段 108 的清洁自愈会动摇 B8-0a 已经过了的闸，所以不该改 / 改了要重审下游 | `b8_inputs_availability.md` §6.6.15.1 与 §6.6.18；`results/b8_0a_gate.md` | **不成立，两层都不成立（2026-08-17）。** 第一层是代数：`pass = ratio_max < 1.0`，`ratio_max` 是合格环上的**最大值**，排样本只降不升，而六档全 PASS，**PASS 翻不成 FAIL**。第二层是实测，比代数更强：**删掉最多 31.25% 的清洁自愈，合格环数与 `ratio_max` 逐档零变化**，因为那些发作从来没通过合格筛选。**这是本站第三次「真实但惰性」的缺陷**（坑 13 的更正删零个对、坑 14 就是为它写的、O28 删三成样本零变化）。**修复照做**，理由是定义（一次支付递延是一个事件）不是影响面，§6.6.15.5 明写不许拿影响面小去追认不必做 |

---

## 五、仍然开着的（该提，这里就是清单）

**这一节是给审查者省时间的：不必自己重新发现一遍。**

| # | 开着的问题 | 登记在哪 | 状态 |
|---|---|---|---|
| O1 | **支撑集收缩的离岸化混杂未排除**，日本站风险最高 | `PROJECT_PLAN_FINGERPRINTS.md` §7 登记 L2 | 零成本第一步：先确认主口径含不含进口中间投入 |
| O2 | **复检 i 反号的解释**：底部注入是可观测的竞争解释，从未进候选 | 同上，登记 L1 | **【2026-08-16 二次更新】共线闸已跑完，判甲可分离，`R = 0.9649`**（`results/ri_l1_gate_results.md`）。年 FE 闸与共线闸**两道都过**。**但本条仍开着**：L1 还要过闸零、闸一、闸三，且它的处理变量并没有被换掉只是被切开，**读法只能是通道间的相对比较，不能是水平主张**。副产物**（同日更正）**：`IRL` `CAN` 缺的是 `eq_tr` 不是财政序列，**L1 主格不掉，仍是 16 国**；掉的是闸门主臂的覆盖，已加只用 `C1` 的旁臂补上（16/560 格 `R = 0.9902`） |
| O3 | ~~**违约瀑布没钉时滞**，任何时序都相容，故现在不是证据~~ | 同上，登记 L3 | **【2026-08-16 已结清，移至 §三 C14】** 开工前闸门判**不开站**，执行 L3 自己写的另一半：瀑布降为并列截面 |
| O4 | **楔子对 R3（NK 粘性工资 / HANK）无区分力**，而它是主承重之一 | 根本性问题审查 §四·9 | 需要一条只有本框架预言的附加约束（注入点交互 / 可达性交互） |
| O5 | **SMD（Sonnenschein–Mantel–Debreu）缺席**，卷二打的是自己重构的「看不见的手」 | 同上 §四·8 | 接进去是净利好，非单连通域那条是框架的真增量 |
| O6 | **per-agent integrability 未检**，它是「房贷那半坍缩成计量」的**充要条件** | `b2_measurement.md` §3.3、§7 | 数理臂唯一承重且有经验内容的未检假设 |
| O7 | ~~**「同层支出等于储蓄」的强形式（零位移）从未量化**；F7 支持的是弱形式~~ | 根本性问题审查追加 §八·7 | **【2026-08-16 陛下裁准改弱形式，已结清，见 §三 C1】** 替换文本五处备妥（`PROJECT_PLAN_FINGERPRINTS.md` §14.2），**待贴入原稿** |
| O8 | **温跃层没有分层规则**，唯一操作化藏在 F7 代码里，原稿只字未提 | 同上 §八·8 | 把 F7 的 Z.1 划分写进卷一·三即可。**【2026-08-16 优先级上升】**卷一·三改弱形式之后正文直接引 F7 的读数，而那个划分仍未写出，读者无法核对 |
| O9 | **卷一·七（代谢天花板与四条排气阀）整章零实证**，而回购/并购数据全公开 | 同上 §八 追加 5 | 账面上最大一块未取的分数（F9/F10 未开工） |
| O10 | **卷三·四「人智边际不衰减」对 Bloom 等的欠条未还**，而卷三第八、九节挂在它上面 | 同上 §八·11 | 弱化为「衰减慢于捕获失效速度」即可，弱版本不冲突且够用 |
| O11 | **覆盖检验只列解释什么，不列禁止什么**，作为自证判据形式无效 | 同上 §八·6 | 每个表面配一句「若框架为假但该表面仍出现，那会是什么原因」 |
| O12 | **卷一·一的 M_a/R_a 机制假设了 1982 年后不存在的货币目标制** | 同上 §八·4 | 换成「盯任何只由活跃侧可观测量构成的目标」，结论不变，等式降为方向陈述 |
| O13 | **粗网格的观测谱与 null 上限未序列化**，故「第二个维度住在 36–49 内部」定不了强弱 | `PROJECT_PLAN.md` §21.11 | 下次动脚本时与 `jobs`、design 块两处一并补 |
| O14 | **F15b 控制 0 类占比后斜率 +3.12 那一版的 p 值未报**，可能是该站唯一功效足够的设定 | `PROJECT_PLAN_FINGERPRINTS.md` §10 | 从 `results/f15b_*.md` 取回 |
| O15 | ~~**卷一·十八的四个截面选择本身触到纪律 1**（特征搜索锚定结论）~~ | 根本性问题审查追加 §八·2 | **【2026-08-16 已结清，移至 §三 C15】** 处置取「降为例示不作证据」这一支 |
| O18 | **少付月份剩下的 46.65% 未命名** | `b8_inputs_availability.md` §6.2.6 与 §6.2.7.5 | 冻结吃掉 53.35%，只付息期、还款安排、结清月份、延滞前缘四个候选已排除。剩下的是**扣掉一个已知成分之后更干净的未知**，但仍未命名 |
| O19 | **C8-1f 的 `MAX_LAG = 6` 封顶了长窗口两列** | `results/b8_c8_1f_freeze_recovery.md` §4 | `L = 12` 与 `L = 24` 与 `L = 7` 同值，不可读。`L = 3` 与 `L = 6` 是实数。提高 `MAX_LAG` 重跑即可，成本是一趟核心表扫描 |
| O16 | **C8-1a 那 1% 的机制未定**：额外还款加每月重算月供，与期限差一个月，两者对 `ω` 的修法完全不同 | `claude-docs/B8_C8-1a裁定_v1.md` §4、§7；`b8_inputs_availability.md` §6.2.3 | C8-1c 已写未跑，判别式是它 §3 那张「下界月供 / 重算月供」的表。**跑前钉死的四条读法在裁定件 §7，跑完不许回头改** |
| O20 | ~~**四个脚本的数是在旧 quiet 口径下出的，待重跑双报**~~ | `b8_inputs_availability.md` §6.2.10.2；`results/b8_quiet_delta.md` | **【2026-08-16 同日结清，且原判据的理由被实测推翻】** 四个已重跑，**数与更正前逐位相同，因为那条更正在六档上删掉零个对**。原条目写的「污染只在高于众数那一侧，p90 读 2.00–2.12 正是它的形状」**是推理不是测量，而且是错的**：没有污染，那个 p90 是巧合被征用成证据。双报由 `b8_quiet_delta.md` 结清而不由那四份结果件结清，**它们打印新数而不打印 `n_dropped_cur_zero`** |
| O22 | **`purpose` 的 `U` 只有布局文档的识别依据，不满足 C0b** | `b8_inputs_availability.md` §6.4.4 | 空白是行为识别的（字段不在），`U` 是「看起来像 unknown」加布局文档，而 C0b 的规矩是字段不由取值长相也不由布局文档识别。按裁定排除、印计数（全样本 14 笔）、**下游不许把 `U` 当成已识别**。量小到任何读数都不可能压在它上面，**但这条不因为量小就结清** |
| O23 | **B8-4b 的 cohort 轴在六个抽样季度上不可比，而不只是薄** | `b8_inputs_availability.md` §6.4.7 | Flex 修改窗口是 2017-2019，2019Q1 那批在窗口里最多一年账龄、完不成三角，2002Q1 那批有十五年。argmin 大半落在 `2019Q1`。**下全年份能把 cohort 补连续，但补不掉账龄与修改窗口的交互**，那是设计层的问题不是样本量问题，开 B8-4b 之前要单独裁 |
| O24 | **递延腿估不出合同月供：不成立，已删** **【2026-08-17 在支付递延这一臂上不成立，判据由 C11-4 给】** | `b8_inputs_availability.md` §6.6.11.6 与 **§6.6.14.5**；`results/b8_c11_deferred_balance.md` §5 | 原登记的 703,504 是**字段 63**（修改带本金宽限）的 on-row 数，不是支付递延人群。**字段 108 的 35,617 笔贷款里，递延前月供已知 34,989（98.24%），全路径覆盖 33,073（92.86%）**，因为 `require_never_deferred=True` 排的是字段 63、从来没排过 108。**B8-3 的主路径对两侧现在都可跑**（修改臂 49,649 环、递延臂 35,659 环）。**残余，且方向反了**：被排掉的 11,128 笔带字段 63 的贷款现在属于**修改臂**，所以覆盖洞挪到了修改臂上，见 §6.6.14.6 与新登记的 O29 |
| O25 | ~~**修改三角的环窗口至今没有定义**~~ | `b8_fannie_slice.md` §17 | **【2026-08-17 已结清，移至 §四 D17】** 注册文字在 §17，另存 `claude-docs/B8_环窗口与曲线规则_预注册_v1.md`。**欠一笔双报**：临时窗口下的环级数字（6,272 / 13,134 / 17,061 / 2,954 / 5,411 / 4,647）要在注册窗口下重跑时并印并给 delta |
| O21 | ~~**三十年期 CMT 在 2002-02 到 2006-02 之间不存在**，而那正是 2002Q1 剩余期限接近 360 个月的窗口~~ | `b8_fannie_slice.md` §17.14–§17.16 | **【2026-08-17 已结清，移至 §四 D18】** 插值 `linear_in_tenor`、超档 `cap`，缺口里的修改 1,506 / 50,958。**残余不为零，而且 2026-08-17 已经触发**：这两条当时够不着任何一行，承重要等 O24 解开。**O24 已解开（92.86% 全路径覆盖），载体已由 C10-4 定到字段 108，`V` 的气球已改成 `63 + 108`，所以气球项第一次有月供已知的行。`b8_cmt_sensitivity` 那张六种构造对比表现在是欠的**，要在带气球且月供已知的行上重跑，**在重跑之前不许引 §17.16 第 1 到 4 条的读数说曲线规则不承重**。**【2026-08-17 同日：前置读数已就位】** `b8_loop_omega` 印「带气球的行数」两列（本行 / 前一行），而 `b8_cmt_sensitivity2.per_row_r` 已改成 `b8_omega.row_residuals` 的薄封装 —— 它原来是逐行残差的**第二份实现**，到当天已在三件事上漂了（余额读 `12 − 63`、气球贴字段 17、走旧的 `nib_` 参数名），**于是那份敏感度扫描量的与环和量的不是同一个量**（§6.6.23.6）。**重跑本身仍然欠着**（§6.6.22.9、`b8_fannie_slice.md` §17.16 已记账） |
| O17 | **字段 64 在六档上零笔为正，而这不是对 64 的识别** | `b8_inputs_availability.md` §6.2.1 | 零与「字段对、样本上确实没有减免」相容，也与「字段错、那一列恰好全零」相容。C0b 的锚点只到 44。现在只记「该列在本样本上恒为零」，**不记「减免恒为零」**。B8-0a(ii) 的定义已按此收窄 |
| O28 | ~~**B8-0a 的闸样本里混着支付递延**~~ | `b8_inputs_availability.md` §6.6.18；`results/b8_0a_gate.md` | **【2026-08-17 已结清，修复已跑，移至 §四 D21】** `find_clean_cures` 加了「字段 108 恒为零」，移除 520 / 807 / 899 / 6,949 / 21,186 / **24,077**（0.17%–**31.25%**）。**合格环与 `ratio_max` 逐档零变化**（19,807 / 6,122 / 6,603 / 5,644 / 10,203 / 6,467；0.400 / 0.399 / 0.400 ×4），六档仍全 PASS。**已发布的每一个数一个字没动**：那些发作本来就没通过闸的合格筛选（递延期间余额不按计划摊还）。**残余**：`require_no_defer=False` 保留可复现旧样本；下游 `b8_c10_contract_move` 的 floor 臂重钉 `1/4 → 1/3` |
| O29 | **`quiet_pairs` 的排除规则与 C8-1 / C11-1 的裁定对不上，两个方向都错** | `b8_inputs_availability.md` §6.6.14.6；`results/b8_c11_deferred_balance.md` §5 | **一**：`require_never_deferred=True` 把曾带**字段 63** 的 11,128 笔整个排出月供估计，而字段 63 现在归**修改臂**，所以排掉的是修改臂自己的样本。**二**：字段-108 的贷款**没被排**，而 `contract_periods` 不在 108 的上升沿切，于是一个合同期横跨递延起点，众数簇把递延前（口径对）与递延后（口径 A，C11-1 已判错）的隐含月供混在一起。**两个口子指向同一个修复**：不排贷款，把隐含月供算在 **`12 − 63 − 108`** 上（C8-1 裁 12 含 63、C11-1 裁 12 含 108，合起来就是计息余额）。**不动它**：那会 re-base B8-0a 与 C8-1 全家，按 R01 要重跑并双报。**与 O28 同族，一起裁** 。**【2026-08-17 C12 量出影响面】** 反向检查：既无 63 也无 108 的 **2,681,230 笔可比贷款 `changed` 全 0、`max` 全 `0.00e+00`**，开关在该无为处无为，是跑出来的。**方向一纯增量已数出来**：新增可估 **9,760 / 11,128（87.71%）**，且 `gained_any` 与 `gained` 六档逐档相等，**没有任何别的贷款获得估计，也就没有别的数被动过。双报是空的。****方向二**：**15,728 / 34,119（46.1%）的字段-108 贷款估计值会变，2019Q1 最大 4.4439（444%）**。**旧数是 C11-1 已判错的数，补印它等于再发一遍错误；C12 那张表本身就是那份双报**（谁变、变多大、为什么）。**【2026-08-17 陛下裁准，两个方向全部接受，已落地】** `quiet_pairs` 两个默认都翻了（`require_never_deferred=False`、`ib_net=True`），旧参数保留可复现。**2026-08-17 同日追加**：`12 − 63 − 108` 这份算术已收成一份，在 `b8_core.zero_interest_split`，`quiet_pairs` 与 `b8_0a_gate` 两处改道，**已验逐位相同**（§6.6.22.6）。**残余**：`b8_c11_deferred_balance` 两处裸调用已钉成旧参数（§6.6.21.3） |
| O26 | ~~**`t_M == t_B` 占已注册环的 76.8%，`modified → current` 这条边在其上给零个月**~~ | `b8_inputs_availability.md` §6.6.8.2；`results/b8_c10_contract_move.md` §1 | **【2026-08-17 已结清，判甲，移至 §四 D19】** C10-1 实跑：A 臂 0.6905–0.9986、B 臂 0.6784–1.0000，**A/B 比 0.957–1.224 六档全同量级**，两者都在噪声底的四位数倍上（底 62 / 919,207 = 6.74e-5）。那 34,621 个是 leg 3 长度为零的修改三角，**照旧进 B8-1**。**残余**：`ω₃` 只在 10,449 个环上可量且是被选择的子样本，**每处引用必带** |
| O27 | ~~**§14.3 与 §14.4 对字段 63 说法不同，`deferred` 层按 ADR 码论证、按字段 63 实现**~~ | `b8_inputs_availability.md` §6.6.11；`results/b8_c10_4_tier_carrier.md` | **【2026-08-17 已结清，判乙，移至 §四 D20】** C10-4：`still`（利率不动**且**期限恰好减一）汇总 **nib 0.0513 对 defer/adr 0.9966**，差 19.4 倍，逐档 7.0–43.3 倍，六档方向一致。配对表：**defer 与 adr 的首个上升沿逐笔同行，35,617 笔零例外**，只在尾部行差 0–149，取覆盖最全者即 108（携带金额，`V` 要）。`nib` 与 `defer` **从不同行**（1,276 笔重叠，同行 0），而 `nib` 的指纹 rate 0.4610 / term 0.8423 **正是 §14.3 leg 2 描述的修改**。**§14.3 与 §14.4 都对且一直都对，错的只有实现切了哪一列。** **残余**：`b8_loops` 与 C10-1/2/3 要全部重跑；新阻塞 C11（`V` 的气球读不到 108 的递延余额） |
| O30 | **B8-2 不跑递延三角的禁令，理由已被本项目自己的数推翻，禁令本身要重新立** | `b8_fannie_slice.md` §14.4；`b8_inputs_availability.md` §6.6.22.9 | **【2026-08-17 新登记】** §14.4 原文写「Deferral exists only in the COVID window」，**而同一节自己的审计数是 32,533 个递延三角里 31,057 在 COVID 窗口内，1,476 在窗口外**。自相矛盾了两节没人看。**禁令留着，理由换掉**：不是「不存在」，而是 1,476 个三角散在三个 COVID 前窗口里撑不起跨窗比较。**这一步是可数的，而「不存在」是靠看的。** **开着的部分**：哪个 COVID 前窗口过不过 `MIN_CELL`，是 B8-2 要读的数，**在环普查按窗口分档跑出来之前不裁**。若有窗口过得了，那么递延臂的跨窗比较就是可跑的，**而 §14.4 现在写的是不跑**。**【2026-08-17 实测，性质变了】** B8-3 的窗口表：递延臂在 pre-crisis / HAMP / Flex 上**六档全部为 0**，非零只在 COVID 与 post-2022。**所以那 1,476 个「窗口之外」的递延三角全部在 COVID 之后，一个都不在之前** ——支付递延作为项目是 COVID 起才有的。**这不是「样本太薄」，是可比窗口只有两个而且不会再多。结构，不是抽样。** 同时暴露 B8-2 的一条：档位 × 窗口是下三角（档位名是收购季度，每一档只能填它活过的窗口），**而窗口与账龄共线，账龄进 `ω`**（余额不进，`ω` 对它零次齐次；剩余期限进）。**B8-2 的跨窗比较必须在档位内做或在账龄上分层**，见 §6.6.29.6 |
| O31 | **`V̂` 一直按新合同定价，而那正是 leg 2；同类地，一条注册过的恒等式在实际实现下是空的** | `b8_inputs_availability.md` §6.6.23.1 与 §6.6.23.3；`b8_fannie_slice.md` §14.2、§17.11 | **【2026-08-17 已发现并已修，登记为方法层的账】** **一**：`r_month` 两条腿共用 `note_pct` 与 `n_now`，而 §14.2 写的是 `V̂` 用 `t−1` 的利率与 `t−1` 的期限减一。安静月份上两者逐位相同，**所以 P1–P5 在两种读法下全过**；分家的地方是修改当月，也就是 leg 2 唯一的一个月。实测 2% 降息 + 120 个月展期：**`+1.670e-03` 对 `-1.792e-01`，两个数量级且符号反**。已修，省略新参数时逐位复现旧行为。**二**：§17.11 那条三腿恒等式在前缀和实现下**望远镜式抵消，`t_M` 取什么都成立**，抓不住它被写下来要抓的那个错位。已加 `replay`（逐月 Python 重算）并在自检里证明两者不是同一条。**开着的部分**：这两条是同一个形状 —— **一条检查在概念上能失败，不等于它在将要采用的实现下能失败**。本项目还有多少断言是这个形状，没有普查过。**普查方法已经有了**（对每条断言做一次它该抓的变异），成本是每条一次自检 |
| O32 | **B8-0b 的底不收敛，判据换成 MAD 比值；换判据这件事本身要被审** | `b8_inputs_availability.md` §6.6.26；`b8_fannie_slice.md` §18.7；`results/b8_0b_floor.md` | **【2026-08-17 已裁，理由是实测】** 清洁自愈臂的 `2·Var` 从 n=100 的 5.3e-09 单调爬到全样本 1.5e-05，**2,900 倍且最后一步还在爬**，p10–p90 倍差长到 293x；同一条臂的 MAD 从 n=100 起平到三位有效数字。**信号臂两个都收敛，问题整个在底那一侧。** `√Z/√N` 在三次站得住的人群选择下走过 1.45 → 54.70 → 1,008。**一个能被人群定义推三个数量级的比值不是信噪比。** 已换成两边同用 MAD，旧口径并排。**开着的三件**：一、MAD 比值的门槛未定（§5 的 `>3` 是给 `√Z/√N` 写的），实测 6,501 / 50,680 / 4,931 离任何合理门槛都很远，不急；二、**Theorem 3 是方差分解而 MAD 不是那条定理里的量**，所以能说「信号的中心在底的中心之上五万倍」，不能说「方差分解成立」；三、「多少环与测量误差不可区分」需要底的**尾巴**而不是中心，是另一个问题。**【同日再更新，底又变了一次，而这次是变对了】** `corr(ω, closed)` 五档 `+1.0000`、一档 `+0.9993`：**清洁自愈的环和就是 `loop_residual_ideal`，一个确定函数，不是噪声**。`N` 改成 `ω − closed`，量出 2.68e-08 到 5.22e-08，而半分钱除以余额中位数 165,000 是 3.03e-08 —— **底是字段 12 的分位取整**。§14.5 那句「must return zero」是错的（P4 一直在断言正确的那句），更正在 §18.8。**新开的一条**：修改臂也带同类的确定性离散分量而它没有闭式解，若与清洁自愈臂同量级则占信号万分之一可忽略，**但那是推断不是测量** |
| O33 | **曲线规则承重，而且是坑 38 自己的更正造成的。`b8_cmt_sensitivity2` 的抵消闸在六档上全部判 DOES NOT CANCEL** | `results/b8_cmt_sensitivity2.md` §1；`b8_fannie_slice.md` §17.16；坑 38 | **【2026-08-17 开，未结】** 该文件全部逻辑建立在一条恒等式上：无递延余额时 `V = LP(bal, i, n)·A(d, n)`，两腿同 `n` 则年金因子恰好抵消、贴现率根本不出现。**那条恒等式在坑 38 之前为真、之后为假。** §14.2 要求 `V̂` 用上一个月的合同，坑 38 照此把 `n_prev = n − 1` 传了进去，于是两腿的 `n` 差一个月，`A(d, n)` 与 `A(d, n−1)` 不再抵消。数值验证：同 `n` 时 `d = 2%` 与 `d = 8%` 给出完全相同的 `+5.129329e-02`（贴现率不出现）；`n` 与 `n−1` 时给出 `+1.23e-03` / `−4.67e-04`，**并在 `d = i` 处恰好过零**。这与 B8-2 leg 2 分解里那条 `k = LP·A` 的 `d < i` 通道**是同一个通道，两个独立站点各自撞到一次**。**实测后果**：无气球行上六种构造的 `r` 极差 2.89e-02 到 1.02e-01，对着 1.24e-14 的界；环级 `p50 / 底` = **10 到 1,010**。**该文件自己的规矩是「§1 不过则下面全部不是测量」，所以 §2 §3 的数按它自己的规矩不可引。** **对判据的影响，逐条估**：B8-3 的 `delta/floor` 是 1.07e5 到 4.18e6，比曲线极差**高二到四个数量级**，且两条路径人群同窗口、`d − i` 差距相近，**安全**；B8-1 的分子分母同受影响，**待查**；**B8-2 最危险**，它的逐期中位数量级 1e-2 到 1e-1，与曲线极差同量级，**一个格的符号可能翻**。**处置**：在第二条曲线规则下重跑 B8-2 并双报，规则对按 §16.11 已经钉死。**在此之前 §17.16 第 1 到 4 条不是「不许引」而是「已知为假」** **【2026-08-17 同日已结，`results/b8_2_curve.md`】** 远角构造（`linear_in_log_tenor` / `linear_in_log_tenor`，两个选择同时换）下重跑：**B8-2 零个格翻号**，两种构造都是 29/29 同号且区间不跨零、五窗口一致；**B8-1 两条臂都查**，分子 B/A = 0.963–0.995，**分母 MAD 底在六档上 A 与 B 五位有效数字完全相同**，比值 2.41e6..6.77e6 → 2.36e6..6.63e6。**风险本来在分母而它没被够到**：清洁自愈不重签合同、字段 17 每月减一，`n_prev == n_now`，年金因子无论曲线是什么都抵消。**O33 对 B8-2 与 B8-1 结清；§17.16 第 1 到 4 条仍然撤回。** 未结的只有「哪条构造是对的」，而 §16.11 在跑之前已经钉死了一条 |
| O34 | `b8_cmt_sensitivity2` §2 的散文已过期，解释的是一个不再存在的零 | `results/b8_cmt_sensitivity2.md` §2 | **【2026-08-17 开，未结】** 它写「`quiet_pairs(require_never_deferred=True)` 排除了曾递延的贷款，所以递延行按构造不可能带已知月供」，而**那个默认值在同一天（§6.6.17）改成了 `False`**，改由 `ib_net` 把递延余额净掉；且那个排除本来只看字段 63、不看 108。实测气球行 41,658 到 521,941，**那个零已经不存在**。散文照 2026-08-17 的裁定**删掉，不划线**。**另外 §2 表里「deferred rows in the file」小于「of those, with a known payment」**（2002Q1 是 29,193 对 41,658），两列口径不同却并排放，**列名要改**：前者数的是字段 63 的行，后者数的是 `63 + 108` 的气球行 |
| O35 | **至少有一笔清洁自愈上年金抵消失效，而 MAD 把它吸收掉了** | `results/b8_2_curve.md` §4 | **【2026-08-17 开，未结，不改任何判决】** 2017Q1 的 `max floor shift` = **3.102e-06，等于该档底 2.695e-08 的 115 倍**；其余五档 1.8e-12 到 1.5e-11，即零。**所以清洁自愈臂上并非处处 `n_prev == n_now`**，机制已知（字段 17 没有每月减一，`b8_0b_floor.freeze_counts` 正是为这件事写的）。**MAD 吸收了它**：底的 A/B 到五位有效数字相同，B8-1 的比值只按分子动。**一个方差型的底不会吸收**，所以这是 §18.7 从 `2·Var` 换到 MAD 那次裁定的**一次追认**，而那次换估计量的理由当时是收敛性、不是稳健性。**待办**：数有多少笔、是哪一批、字段 17 在那批上是怎么走的。**在数出来之前不许说「清洁自愈臂上抵消处处成立」** |

---

## 六、怎么往这份表加东西

1. **只加有出处的。** 文档 + 节号，或结果文件 + 字段名。
2. **裁定用四种之一**：不成立 / 部分成立 / 成立 / 事实错误。含糊的不进表。
3. **「还剩什么」栏是承重的**，因为多数反驳是部分成立的，把残余写清楚才防得住下一次。
4. **第五节的条目结清之后移到前四节**，并注明结清日期与出处。**不删。**
5. 新反驳若在这份表里找不到对应行，那它就是新的，**照提**。
